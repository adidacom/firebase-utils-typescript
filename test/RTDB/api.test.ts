// modules
import * as assert from 'assert'
import {getNewsfeed} from '../../RTDB/api'

describe('api', () => {

	/* don't test, it takes too long
	it('getNewsfeed should get newsfeed', async () => {
		
		const newsfeed = await getNewsfeed('awesomemarc31977', 2)

		assert.equal(newsfeed.length, 50)

	})
	*/


})

